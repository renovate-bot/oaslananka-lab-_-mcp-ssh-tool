name: CI

on:
  push:
    branches:
      - main
      - "chore/**"
      - "feature/**"
      - "fix/**"
  pull_request:
    branches:
      - main
  merge_group:
    types:
      - checks_requested
  workflow_dispatch:

permissions:
  contents: read

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
  DOPPLER_PROJECT: all
  DOPPLER_CONFIG: main
  HAS_DOPPLER_TOKEN: ${{ secrets.DOPPLER_TOKEN != '' }}

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

defaults:
  run:
    shell: bash

jobs:
  quality:
    name: Quality Gates
    if: ${{ github.repository == 'oaslananka-lab/mcp-ssh-tool' && (github.event_name != 'pull_request' || github.event.pull_request.draft == false) }}
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd
        with:
          persist-credentials: false

      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e
        with:
          node-version: 24.14.1
          package-manager-cache: false

      - name: Enable pnpm
        run: |
          set -euo pipefail
          corepack enable
          corepack prepare pnpm@11.0.8 --activate

      - name: Install
        run: pnpm install --frozen-lockfile

      - name: Quality gate parity
        run: pnpm run check:quality

  unit:
    name: Unit Tests (Node ${{ matrix.node }})
    if: ${{ github.repository == 'oaslananka-lab/mcp-ssh-tool' && (github.event_name != 'pull_request' || github.event.pull_request.draft == false) }}
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        node:
          - 22.22.2
          - 24.14.1
    steps:
      - uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd
        with:
          persist-credentials: false

      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e
        with:
          node-version: ${{ matrix.node }}
          package-manager-cache: false

      - name: Enable pnpm
        run: |
          set -euo pipefail
          corepack enable
          corepack prepare pnpm@11.0.8 --activate

      - name: Install
        run: pnpm install --frozen-lockfile

      - name: Unit coverage
        run: pnpm run test:coverage

      - name: Upload JUnit results
        if: always()
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f
        with:
          name: junit-node-${{ matrix.node }}
          path: test-results/**/*.xml
          if-no-files-found: warn

      - name: Upload coverage artifact
        if: always() && matrix.node == '24.14.1'
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f
        with:
          name: coverage
          path: coverage
          if-no-files-found: ignore

      - name: Install Doppler CLI
        if: matrix.node == '24.14.1' && env.HAS_DOPPLER_TOKEN == 'true'
        run: curl -Ls https://cli.doppler.com/install.sh | sudo sh

      - name: Export Codecov token from Doppler
        if: matrix.node == '24.14.1' && env.HAS_DOPPLER_TOKEN == 'true'
        env:
          DOPPLER_TOKEN: ${{ secrets.DOPPLER_TOKEN }}
        run: |
          doppler run -- bash <<'BASH'
          set -euo pipefail
          test -n "${CODECOV_TOKEN:-}"
          echo "::add-mask::$CODECOV_TOKEN"
          printf 'CODECOV_TOKEN=%s\n' "$CODECOV_TOKEN" >> "$GITHUB_ENV"
          BASH

      - name: Upload coverage to Codecov
        if: matrix.node == '24.14.1' && env.HAS_DOPPLER_TOKEN == 'true'
        uses: codecov/codecov-action@57e3a136b779b570ffcdbf80b3bdc90e7fab3de2
        with:
          files: coverage/lcov.info
          disable_search: true
          fail_ci_if_error: false

  integration:
    name: SSH Integration
    if: ${{ github.repository == 'oaslananka-lab/mcp-ssh-tool' && (github.event_name != 'pull_request' || github.event.pull_request.draft == false) }}
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd
        with:
          persist-credentials: false

      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e
        with:
          node-version: 24.14.1
          package-manager-cache: false

      - name: Enable pnpm
        run: |
          set -euo pipefail
          corepack enable
          corepack prepare pnpm@11.0.8 --activate

      - name: Install
        run: pnpm install --frozen-lockfile

      - name: Start SSH fixture
        run: docker compose up -d ssh-server

      - name: Run integration tests
        run: pnpm run test:integration
        env:
          RUN_SSH_INTEGRATION: "1"
          TEST_SSH_HOST: localhost
          TEST_SSH_PORT: "2222"
          TEST_SSH_USER: testuser
          TEST_SSH_PASS: testpass

      - name: Stop SSH fixture
        if: always()
        run: docker compose down --remove-orphans

      - name: Upload integration JUnit results
        if: always()
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f
        with:
          name: junit-integration
          path: test-results/**/*.xml
          if-no-files-found: warn

  package:
    name: Build, SBOM, and Pack
    if: ${{ github.repository == 'oaslananka-lab/mcp-ssh-tool' && (github.event_name != 'pull_request' || github.event.pull_request.draft == false) }}
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    needs:
      - quality
      - unit
      - integration
    steps:
      - uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd
        with:
          persist-credentials: false

      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e
        with:
          node-version: 24.14.1
          package-manager-cache: false

      - name: Enable pnpm
        run: |
          set -euo pipefail
          corepack enable
          corepack prepare pnpm@11.0.8 --activate

      - name: Install
        run: pnpm install --frozen-lockfile

      - name: Package parity
        run: pnpm run check:package

      - name: Generate CycloneDX SBOM and package hash
        run: |
          set -euo pipefail
          mkdir -p artifacts
          pnpm run sbom
          PACKAGE_FILE="$(
            pnpm pack --pack-destination artifacts --json |
              node -e "const path = require('node:path'); let d=''; process.stdin.on('data', c => d += c); process.stdin.on('end', () => { const p = JSON.parse(d); const r = Array.isArray(p) ? p[0] : p; console.log(path.basename(r.filename || r.name || r.path)); });"
          )"
          sha256sum "artifacts/$PACKAGE_FILE" > "artifacts/$PACKAGE_FILE.sha256"

      - name: Upload release verification artifacts
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f
        with:
          name: release-verification
          path: artifacts

  docker:
    name: Docker Smoke
    if: ${{ github.repository == 'oaslananka-lab/mcp-ssh-tool' && (github.event_name != 'pull_request' || github.event.pull_request.draft == false) }}
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    needs:
      - package
    steps:
      - uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd
        with:
          persist-credentials: false

      - name: Build production image
        run: docker build -t mcp-ssh-tool:ci .

      - name: Smoke CLI
        run: |
          docker run --rm mcp-ssh-tool:ci --version
          docker run --rm mcp-ssh-tool:ci --help
